main() {
  // 关键字 bool
  // 布尔值的类型只有true和false两种
  // 与其他语言不同： 例如javaScript中 布尔非空都是true
  bool flag = true;
  // bool flag2 = "123"; 错误写法
  print(flag);
  if (flag) {}
  // if ('111'){

  // }  错误写法
}
